@using  WebAspCore.ViewModel.ViewModels.Products
@model List<ProductCategoryViewModel>
<ul class="list">
    <ul class="list">
        @*@foreach (var item in Model.Where(x => string.IsNullOrEmpty(x.ParentId.ToString())))*@
        @foreach (var item in Model)
        {
            <li>
                <button class="btnCategory" style="border:none;background:none" data-id=@item.Id> @item.Name</button>
                @*@if (Model.Any(x => x.ParentId == item.Id))
                    {
                        <ul class="nav child_menu">
                            @foreach (var jitem in Model.Where(x => x.ParentId == item.Id))
                            {
                                <li><a href="#">@jitem.Name</a></li>

                            }
                        </ul>
                    }*@
            </li>
        }
    </ul>

</ul>
